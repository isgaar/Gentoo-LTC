# vim: set ft=sh ts=4 sw=4 sts=-1 noet:
# This file will be interpreted by /bin/bash.
#
# Hardware profile: Lenovo ThinkCentre M75s Gen 2
# CPU: AMD Ryzen 5 5600G with Radeon Graphics / Cezanne / znver3, 6 cores / 12 threads
# GPU: AMD Cezanne Radeon Vega iGPU (amdgpu / radeonsi / radv)
# Wi-Fi: Intel Wi-Fi 6 AX200 (reused from previous device)
# Storage interface: auto-detect exactly one NVMe namespace (reused from previous device)


################################################
# Disk configuration

# Automatic mode for the replacement NVMe.
# The installer will abort unless exactly one /dev/nvme*n* disk is present.
# To override manually, replace "auto-nvme" with a real disk, e.g. /dev/nvme0n1.
TARGET_DISK="auto-nvme"

function assert_uefi_boot() {
	[[ -d /sys/firmware/efi ]] \
		|| die "This profile is UEFI-only. Reboot the Gentoo USB in UEFI mode."
}

function resolve_target_disk() {
	if [[ "$TARGET_DISK" != "auto-nvme" ]]; then
		echo "$TARGET_DISK"
		return
	fi

	local disks=()
	local dev
	for dev in /dev/nvme*n[0-9]; do
		[[ -b "$dev" ]] \
			&& disks+=("$dev")
	done

	[[ ${#disks[@]} -gt 0 ]] \
		|| die "No NVMe disk was detected. This profile only installs to NVMe."
	[[ ${#disks[@]} -eq 1 ]] \
		|| die "Auto NVMe install requires exactly one NVMe disk. Found: ${disks[*]}"

	echo "${disks[0]}"
}

function disk_configuration() {
	assert_uefi_boot
	TARGET_DISK="$(resolve_target_disk)"
	create_classic_single_disk_layout swap=16GiB type=efi luks=true root_fs=btrfs "$TARGET_DISK"
}

function before_prepare_environment() {
	assert_uefi_boot

	local resolved
	resolved="$(readlink -f "$TARGET_DISK")" \
		|| die "Could not resolve TARGET_DISK '$TARGET_DISK'."
	[[ -b "$resolved" ]] \
		|| die "TARGET_DISK '$TARGET_DISK' is not a block device."
	[[ "$resolved" == /dev/nvme*n[0-9] ]] \
		|| die "TARGET_DISK '$TARGET_DISK' must resolve to a whole NVMe disk, not a partition."

	einfo "Selected NVMe target disk: $TARGET_DISK"
}


################################################
# System configuration

# systemd requires the static/transient hostname to use lowercase DNS labels.
# PRETTY_HOSTNAME is the user-facing name shown by compatible desktop tools.
HOSTNAME="gentooltc"
PRETTY_HOSTNAME="Gentoo-LTC"
TIMEZONE="America/Mexico_City"
KEYMAP="la-latin1"
KEYMAP_INITRAMFS="$KEYMAP"
FREETYPE_PROPERTIES_VALUE="cff:no-stem-darkening=0 autofitter:no-stem-darkening=0"

LOCALES="
C.UTF-8 UTF-8
en_US.UTF-8 UTF-8
es_MX.UTF-8 UTF-8
"
LOCALE="es_MX.UTF-8"

# NetworkManager is enabled in after_install for laptop Wi-Fi ergonomics.
SYSTEMD_NETWORKD=false
SYSTEMD_NETWORKD_INTERFACE_NAME="en*"
SYSTEMD_NETWORKD_DHCP=true
SYSTEMD_NETWORKD_ADDRESSES=("192.168.1.100/32" "fd00::1/64")
SYSTEMD_NETWORKD_GATEWAY="192.168.1.1"
SYSTEMD_INITRAMFS_SSHD=false


################################################
# Gentoo configuration

PORTAGE_SYNC_TYPE="git"
PORTAGE_GIT_FULL_HISTORY=false
PORTAGE_GIT_MIRROR="https://anongit.gentoo.org/git/repo/sync/gentoo.git"
GENTOO_MIRROR="https://distfiles.gentoo.org"

GENTOO_ARCH="amd64"
GENTOO_SUBARCH=""
STAGE3_VARIANT="systemd"
STAGE3_BASENAME="stage3-$GENTOO_ARCH-$STAGE3_VARIANT"
STAGE3_BASENAME_CUSTOM=$([[ $STAGE3_VARIANT == *x32* ]] && echo "stage3-$STAGE3_VARIANT" || echo "stage3-$GENTOO_SUBARCH-$STAGE3_VARIANT")
SYSTEMD=$([[ $STAGE3_VARIANT == *systemd* ]] && echo "true" || echo "false")
MUSL=$([[ $STAGE3_VARIANT == *musl* ]] && echo "true" || echo "false")

USE_PORTAGE_TESTING=false
# mirrorselect can abort on malformed output returned by netselect.  The
# configured Gentoo mirror is reliable, so keep the installation noninteractive
# and let Portage use it directly.
SELECT_MIRRORS=false
SELECT_MIRRORS_LARGE_FILE=false

# Ryzen 5 5600G: GCC resolves -march=native to znver3 (Cezanne / Zen 3, 6 cores / 12 threads).
PORTAGE_COMMON_FLAGS="-march=znver3 -O2 -pipe"
PORTAGE_MAKEOPTS="-j12 -l12"
PORTAGE_EMERGE_DEFAULT_OPTS="--jobs=4 --load-average=12 --with-bdeps=y"
PORTAGE_FEATURES="parallel-fetch buildpkg"
PORTAGE_USE="X wayland vulkan vaapi vdpau opencl opengl pipewire bluetooth wifi networkmanager dbus udev systemd screencast semantic-desktop"
PORTAGE_ACCEPT_LICENSE="* -@EULA"
PORTAGE_GRUB_PLATFORMS="efi-64"
PORTAGE_CPU_FLAGS_X86="aes avx avx2 bmi1 bmi2 f16c fma3 mmx mmxext pclmul popcnt rdrand sha sse sse2 sse3 sse4_1 sse4_2 sse4a ssse3 vaes vpclmulqdq"
PORTAGE_VIDEO_CARDS="amdgpu radeonsi"
PORTAGE_INPUT_DEVICES="libinput"
PORTAGE_L10N="en en-US es es-MX ja ja-JP ko ko-KR zh zh-CN zh-TW"
PORTAGE_LINGUAS="en en_US es es_MX ja ja_JP ko ko_KR zh zh_CN zh_TW"

# Force early initramfs support for the NVMe root disk and AMDGPU KMS.
DRACUT_FORCE_DRIVERS=(amdgpu nvme)


################################################
# Additional laptop packages

ADDITIONAL_PACKAGES=(
	app-admin/sudo
	app-arch/7zip
	app-arch/unrar
	app-arch/unzip
	app-arch/zip
	app-emulation/libvirt
	app-emulation/qemu
	app-emulation/virt-manager
	app-emulation/virt-viewer
	app-emulation/virtualbox
	app-misc/fastfetch
	app-portage/cpuid2cpuflags
	dev-util/vulkan-tools
	gui-apps/grim
	gui-apps/mako
	gui-apps/slurp
	gui-apps/swaylock
	gui-apps/waybar
	gui-apps/wl-clipboard
	gui-apps/wofi
	gui-libs/xdg-desktop-portal-wlr
	gui-wm/sway
	kde-apps/ark
	kde-apps/dolphin
	kde-apps/kio-extras
	kde-apps/konsole
	kde-plasma/bluedevil
	kde-plasma/breeze-gtk
	kde-plasma/kde-gtk-config
	kde-plasma/plasma-meta
	kde-plasma/discover
	kde-plasma/plasma-pa
	kde-plasma/xdg-desktop-portal-kde
	media-fonts/fontawesome
	media-fonts/jetbrains-mono
	media-fonts/noto
	media-fonts/noto-cjk
	media-fonts/noto-emoji
	media-libs/alsa-ucm-conf
	media-libs/libva
	media-libs/mesa
	media-libs/vulkan-loader
	media-plugins/gst-plugins-vaapi
	media-sound/alsa-utils
	media-video/ffmpeg
	media-video/libva-utils
	media-video/mpv
	media-video/pipewire
	media-video/wireplumber
	net-misc/networkmanager
	net-misc/rsync
	net-wireless/bluez
	net-wireless/iwd
	sys-apps/dbus
	sys-apps/flatpak
	sys-apps/xdg-desktop-portal
	sys-apps/xdg-desktop-portal-gtk
	sys-apps/pciutils
	sys-apps/ripgrep
	sys-apps/usbutils
	sys-auth/rtkit
	sys-boot/grub
	sys-power/power-profiles-daemon
	sys-process/btop
	x11-apps/mesa-progs
	x11-base/xwayland
	x11-drivers/xf86-video-amdgpu
	x11-libs/libvdpau
	x11-misc/vdpauinfo
	x11-misc/sddm
	x11-misc/xdg-user-dirs
	x11-terms/kitty
	# Use Mozilla's generic binary while the base system, graphics stack,
	# desktop environments and kernel continue to be built from source.
	www-client/firefox-bin
)

ENABLE_SSHD=false
# Keep the generic Gentoo binhost disabled. Binary software is selected
# explicitly with -bin atoms (currently Firefox) so core packages are built.
ENABLE_BINPKG=false
KERNEL_TYPE=source
ROOT_SSH_AUTHORIZED_KEYS=""
GRUB_THEME_NAME="gentoo-hp-zorin"
GRUB_THEME_SOURCE="$GENTOO_INSTALL_REPO_DIR/contrib/grub/themes/$GRUB_THEME_NAME"


################################################
# User configuration

# Create one login user interactively. The username, password and sudo access
# are requested during installation and are never stored in this file.
INSTALL_USER=""
INSTALL_USER_SHELL="/bin/bash"
INSTALL_USER_ADMIN="ask"
INSTALL_USER_CONFIG_READY=false
INSTALL_USER_GROUPS=(audio video input plugdev render kvm libvirt vboxusers)
INSTALL_USER_ADMIN_GROUPS=(wheel)
INSTALL_KDE_CUSTOM_PROFILE=true

function configure_dracut_boot_support() {
	[[ "${KERNEL_TYPE:-bin}" == "source" ]] \
		|| die "Gentoo-HP kernel branding requires KERNEL_TYPE=source"

	install -d -m0755 -o root -g root \
			/etc/dracut.conf.d \
			/etc/kernel/config.d \
			/etc/kernel/install.d \
			/etc/kernel/postinst.d \
			/usr/local/sbin \
		|| die "Could not create Dracut/kernel hook directories"
	install -m0644 -o root -g root \
			"$GENTOO_INSTALL_REPO_DIR/contrib/kernel/config.d/99-gentoo-hp-localversion.config" \
			/etc/kernel/config.d/99-gentoo-hp-localversion.config \
		|| die "Could not install persistent kernel localversion configuration"
	install -m0644 -o root -g root \
			"$GENTOO_INSTALL_REPO_DIR/contrib/dracut/90-gentoo-hp.conf" \
			/etc/dracut.conf.d/90-gentoo-hp.conf \
		|| die "Could not install persistent Dracut configuration"
	install -m0755 -o root -g root \
			"$GENTOO_INSTALL_REPO_DIR/contrib/bin/gentoo-hp-update-boot" \
			/usr/local/sbin/gentoo-hp-update-boot \
		|| die "Could not install the Gentoo-HP boot updater"
	install -m0755 -o root -g root \
			"$GENTOO_INSTALL_REPO_DIR/contrib/kernel/postinst.d/95-gentoo-hp-esp.install" \
			/etc/kernel/install.d/95-gentoo-hp-esp.install \
		|| die "Could not install the systemd kernel-install hook"
	install -m0755 -o root -g root \
			"$GENTOO_INSTALL_REPO_DIR/contrib/kernel/postinst.d/95-gentoo-hp-esp.install" \
			/etc/kernel/postinst.d/95-gentoo-hp-esp.install \
		|| die "Could not install the legacy kernel post-install hook"
}

function after_configure_portage() {
	if [[ -n "${PORTAGE_GRUB_PLATFORMS-}" ]]; then
		printf 'GRUB_PLATFORMS=%s\n' "${PORTAGE_GRUB_PLATFORMS@Q}" >> /etc/portage/make.conf \
			|| die "Could not write GRUB_PLATFORMS to make.conf"
	fi
	echo "net-misc/networkmanager iwd" > /etc/portage/package.use/networkmanager \
		|| die "Could not write NetworkManager package.use"
	cat > /etc/portage/package.use/grub <<'EOF' || die "Could not write GRUB package.use"
sys-boot/grub device-mapper fonts themes
EOF
	echo "sys-power/power-profiles-daemon bash-completion" > /etc/portage/package.use/power-profiles-daemon \
		|| die "Could not write power-profiles-daemon package.use"
	echo "gui-wm/sway swaybar swaynag wallpapers X" > /etc/portage/package.use/sway \
		|| die "Could not write Sway package.use"
	echo "gui-apps/waybar network wifi tray logind pipewire" > /etc/portage/package.use/waybar \
		|| die "Could not write Waybar package.use"
	echo "media-fonts/fontawesome otf -ttf" > /etc/portage/package.use/fontawesome \
		|| die "Could not write Font Awesome package.use"
	echo "x11-terms/kitty X wayland" > /etc/portage/package.use/kitty \
		|| die "Could not write Kitty package.use"
	echo "gui-libs/xdg-desktop-portal-wlr systemd" > /etc/portage/package.use/xdg-desktop-portal-wlr \
		|| die "Could not write xdg-desktop-portal-wlr package.use"
	echo "kde-apps/ark zip" > /etc/portage/package.use/ark \
		|| die "Could not write Ark package.use"
cat > /etc/portage/package.use/flatpak <<'EOF' || die "Could not write Flatpak package.use"
kde-plasma/discover flatpak
media-video/pipewire flatpak
sys-apps/flatpak policykit seccomp systemd X
sys-apps/xdg-desktop-portal flatpak
sys-apps/xdg-desktop-portal-gtk wayland X
EOF
	cat > /etc/portage/package.use/audio-bluetooth <<'EOF' || die "Could not write audio/Bluetooth package.use"
media-video/pipewire bluetooth dbus pipewire-alsa sound-server systemd
net-wireless/bluez obex readline systemd udev
sys-auth/rtkit systemd
EOF
	cat > /etc/portage/package.use/virtualization <<'EOF' || die "Could not write virtualization package.use"
app-emulation/libvirt caps libvirtd nls policykit qemu udev virtiofsd virt-network
app-emulation/qemu aio alsa curl fdt gtk io-uring opengl pipewire pulseaudio seccomp slirp spice usb usbredir vhost-net virgl virtfs vnc wayland X xattr zstd qemu_softmmu_targets_x86_64
app-emulation/virt-manager gui policykit
app-emulation/virtualbox alsa dbus gui opengl pulseaudio sdk sdl udev
EOF
	echo "kde-plasma/plasma-meta display-manager networkmanager systemd bluetooth crypt flatpak" > /etc/portage/package.use/plasma-meta \
		|| die "Could not write Plasma package.use"
	echo "x11-misc/sddm systemd X" > /etc/portage/package.use/sddm \
		|| die "Could not write SDDM package.use"
	cat > /etc/portage/package.use/desktop-required-use <<'EOF' || die "Could not write desktop REQUIRED_USE package.use"
# These flags are required by the selected Plasma, PipeWire and VirtualBox stack.
# Keep them package-scoped instead of changing USE globally.
media-libs/libcanberra alsa
media-libs/freetype harfbuzz
media-libs/libsdl2 gles2
sys-apps/systemd policykit
EOF
	cat > /etc/portage/package.use/fonts-unicode <<'EOF' || die "Could not write Unicode fonts package.use"
media-fonts/noto X extra
media-fonts/noto-cjk X
media-fonts/noto-emoji X icons
EOF
	echo "kde-apps/kio-extras mtp" > /etc/portage/package.use/kio-extras \
		|| die "Could not write kio-extras package.use"
	cat > /etc/portage/package.use/gpu-amd-cezanne <<'EOF' || die "Could not write AMD Cezanne GPU package.use"
dev-util/vulkan-tools X wayland cube
media-libs/mesa X wayland opengl vaapi vulkan opencl proprietary-codecs zstd llvm
media-libs/vulkan-loader X wayland
media-plugins/gst-plugins-vaapi X wayland egl opengl drm gles2
media-video/ffmpeg X vaapi vdpau vulkan opencl opengl
media-video/libva-utils X wayland vainfo
media-video/mpv X wayland vaapi vdpau vulkan drm egl libmpv lua pipewire
x11-apps/mesa-progs X wayland vulkan
x11-drivers/xf86-video-amdgpu udev
x11-libs/libvdpau dri
EOF
	# Compatibility link for existing scripts
	ln -sfn gpu-amd-cezanne /etc/portage/package.use/gpu-amd-renoir \
		|| cp -f /etc/portage/package.use/gpu-amd-cezanne /etc/portage/package.use/gpu-amd-renoir
	echo "app-arch/unrar unRAR" >> /etc/portage/package.license \
		|| die "Could not accept the unRAR license for app-arch/unrar"

	configure_dracut_boot_support
}

function validate_install_username() {
	local username="$1"

	[[ -n "$username" ]] \
		|| { ewarn "El nombre de usuario no puede estar vacio."; return 1; }
	[[ "$username" != "root" ]] \
		|| { ewarn "No uses 'root' como usuario normal."; return 1; }
	[[ ${#username} -le 32 ]] \
		|| { ewarn "El nombre de usuario debe tener 32 caracteres o menos."; return 1; }
	[[ "$username" =~ ^[a-z_][a-z0-9_-]*[$]?$ ]] \
		|| { ewarn "Usa minusculas, numeros, guion bajo o guion; debe iniciar con letra minuscula o guion bajo."; return 1; }
}

function normalize_install_user_admin() {
	case "${INSTALL_USER_ADMIN,,}" in
		true|yes|y|1)
			INSTALL_USER_ADMIN=true
			;;
		false|no|n|0)
			INSTALL_USER_ADMIN=false
			;;
		ask|"")
			if ask "Quieres que '$INSTALL_USER' sea administrador con sudo (grupo wheel)?"; then
				INSTALL_USER_ADMIN=true
			else
				INSTALL_USER_ADMIN=false
			fi
			;;
		*)
			die "INSTALL_USER_ADMIN must be true, false or ask"
			;;
	esac
}

function prompt_install_user_configuration() {
	[[ "${INSTALL_USER_CONFIG_READY:-false}" == "true" ]] \
		&& return 0

	if [[ -z "$INSTALL_USER" ]]; then
		while true; do
			flush_stdin
			read -r -p "Nombre del usuario normal que quieres crear: " INSTALL_USER \
				|| die "Error in read"
			validate_install_username "$INSTALL_USER" \
				&& break
		done
	else
		validate_install_username "$INSTALL_USER" \
			|| die "Invalid INSTALL_USER '$INSTALL_USER'"
	fi

	normalize_install_user_admin

	if [[ "$INSTALL_USER_ADMIN" == "true" ]]; then
		einfo "El usuario '$INSTALL_USER' tendra sudo (grupo wheel)."
	else
		ewarn "El usuario '$INSTALL_USER' no tendra sudo. Asigna contrasena de root cuando el instalador pregunte."
	fi

	INSTALL_USER_CONFIG_READY=true
}

function before_install() {
	prompt_install_user_configuration
}

function configure_admin_user() {
	prompt_install_user_configuration

	local requested_groups=("${INSTALL_USER_GROUPS[@]}")
	if [[ "$INSTALL_USER_ADMIN" == "true" ]]; then
		requested_groups+=("${INSTALL_USER_ADMIN_GROUPS[@]}")
	fi

	local existing_groups=()
	local group
	for group in "${requested_groups[@]}"; do
		if getent group "$group" > /dev/null; then
			existing_groups+=("$group")
		elif [[ "$group" == "wheel" ]]; then
			groupadd wheel \
				|| die "Could not create wheel group"
			existing_groups+=("$group")
		else
			ewarn "Group '$group' does not exist; skipping it for $INSTALL_USER"
		fi
	done

	local groups_csv
	groups_csv="$(IFS=,; echo "${existing_groups[*]}")"
	local group_options=()
	[[ -n "$groups_csv" ]] \
		&& group_options=(--groups "$groups_csv")
	local primary_group="$INSTALL_USER"

	if ! getent group "$primary_group" > /dev/null; then
		groupadd "$primary_group" \
			|| die "Could not create primary group '$primary_group'"
	fi

	if id "$INSTALL_USER" > /dev/null 2>&1; then
		usermod --gid "$primary_group" "${group_options[@]}" --shell "$INSTALL_USER_SHELL" "$INSTALL_USER" \
			|| die "Could not update user '$INSTALL_USER'"
	else
		useradd --create-home --shell "$INSTALL_USER_SHELL" --gid "$primary_group" "${group_options[@]}" "$INSTALL_USER" \
			|| die "Could not create user '$INSTALL_USER'"
	fi

	if [[ "$INSTALL_USER_ADMIN" == "true" ]]; then
		install -d -m0750 -o root -g root /etc/sudoers.d \
			|| die "Could not create /etc/sudoers.d"
		printf '%%wheel ALL=(ALL:ALL) ALL\n' > /etc/sudoers.d/00-wheel \
			|| die "Could not write wheel sudoers file"
		chown root:root /etc/sudoers.d/00-wheel \
			|| die "Could not chown wheel sudoers file"
		chmod 0440 /etc/sudoers.d/00-wheel \
			|| die "Could not chmod wheel sudoers file"
		visudo -cf /etc/sudoers.d/00-wheel \
			|| die "Generated sudoers file is invalid"
	else
		einfo "Omitiendo sudo para el usuario '$INSTALL_USER'."
	fi

	einfo "Define la contrasena para el usuario '$INSTALL_USER'"
	passwd "$INSTALL_USER" \
		|| die "Could not set password for '$INSTALL_USER'"
}

function configure_user_bashrc() {
	[[ -n "$INSTALL_USER" ]] \
		|| return 0

	local install_user_group
	install_user_group="$(id -gn "$INSTALL_USER")" \
		|| die "Could not determine primary group for '$INSTALL_USER'"

	install -d -m0755 -o "$INSTALL_USER" -g "$install_user_group" "/home/$INSTALL_USER/.bashrc.d" \
		|| die "Could not create bashrc.d directory"
	cat > "/home/$INSTALL_USER/.bashrc" <<'EOF'
# Configuracion de Bash del usuario.

# Definiciones globales.
if [[ -f /etc/bash/bashrc ]]; then
	. /etc/bash/bashrc
elif [[ -f /etc/bashrc ]]; then
	. /etc/bashrc
fi

# Rutas personales.
function prepend_path_once() {
	case ":$PATH:" in
		*":$1:"*) ;;
		*) PATH="$1:$PATH" ;;
	esac
}

prepend_path_once "$HOME/bin"
prepend_path_once "$HOME/.local/bin"
prepend_path_once "$HOME/.opencode/bin"
export PATH
unset -f prepend_path_once

# Ajustes para sesiones graficas.
export NO_AT_BRIDGE=1

# Fragmentos locales.
if [[ -d "$HOME/.bashrc.d" ]]; then
	for rc in "$HOME"/.bashrc.d/*; do
		[[ -f "$rc" ]] && . "$rc"
	done
fi
unset rc

# Ajustes interactivos.
if [[ $- == *i* ]]; then
	bind "set completion-ignore-case on"
	shopt -s nocaseglob cdspell dirspell
fi
EOF
	chown "$INSTALL_USER:$install_user_group" "/home/$INSTALL_USER/.bashrc" \
		|| die "Could not chown user bashrc"
	chmod 0644 "/home/$INSTALL_USER/.bashrc" \
		|| die "Could not chmod user bashrc"
}

function configure_sddm() {
	install -d -m0755 -o root -g root /etc/sddm.conf.d \
		|| die "Could not create /etc/sddm.conf.d"
	cat > /etc/sddm.conf.d/10-gentoo-hp.conf <<'EOF'
[General]
InputMethod=

[Users]
MaximumUid=65000
MinimumUid=1000
EOF
	chown root:root /etc/sddm.conf.d/10-gentoo-hp.conf \
		|| die "Could not chown SDDM config"
	chmod 0644 /etc/sddm.conf.d/10-gentoo-hp.conf \
		|| die "Could not chmod SDDM config"
}

function configure_grub_bootloader() {
	[[ "$IS_EFI" == "true" ]] \
		|| die "GRUB theme profile is UEFI-only."
	[[ -f "$GRUB_THEME_SOURCE/theme.txt" ]] \
		|| die "GRUB theme source not found: $GRUB_THEME_SOURCE"

	local grub_theme_dir="/boot/efi/grub/themes/$GRUB_THEME_NAME"
	local grub_cmdline
	grub_cmdline="$(get_cmdline)" \
		|| die "Could not build GRUB kernel cmdline"

	einfo "Instalando GRUB UEFI con tema $GRUB_THEME_NAME"
	try grub-install \
		--target=x86_64-efi \
		--efi-directory=/boot/efi \
		--boot-directory=/boot/efi \
		--bootloader-id=Gentoo-LTC \
		--recheck

	install -d -m0755 -o root -g root "$grub_theme_dir" \
		|| die "Could not create GRUB theme directory"
	cp -a "$GRUB_THEME_SOURCE/." "$grub_theme_dir/" \
		|| die "Could not install GRUB theme"
	chown -R root:root "$grub_theme_dir" \
		|| die "Could not chown GRUB theme"
	find "$grub_theme_dir" -type d -exec chmod 0755 {} + \
		|| die "Could not chmod GRUB theme directories"
	find "$grub_theme_dir" -type f -exec chmod 0644 {} + \
		|| die "Could not chmod GRUB theme files"

	cat > /etc/default/grub <<EOF
# Configuracion de GRUB para Gentoo-LTC.
GRUB_DEFAULT=0
GRUB_TIMEOUT=5
GRUB_DISTRIBUTOR="Gentoo-LTC"
GRUB_TERMINAL_OUTPUT="gfxterm"
GRUB_GFXMODE="auto"
GRUB_GFXPAYLOAD_LINUX="keep"
GRUB_THEME="/boot/efi/grub/themes/$GRUB_THEME_NAME/theme.txt"
GRUB_CMDLINE_LINUX="$grub_cmdline"
EOF
	chown root:root /etc/default/grub \
		|| die "Could not chown /etc/default/grub"
	chmod 0644 /etc/default/grub \
		|| die "Could not chmod /etc/default/grub"

	cat > /boot/efi/grub/grub.cfg <<EOF
set default=0
set timeout=5
set timeout_style=menu

if loadfont unicode; then
	set gfxmode=auto
	insmod all_video
	insmod gfxterm
	insmod png
	terminal_output gfxterm
fi

set theme=\$prefix/themes/$GRUB_THEME_NAME/theme.txt
export theme

menuentry "Gentoo-LTC" --class gentoo --class gnu-linux --class os {
	set gfxpayload=keep
	linux /vmlinuz.efi $grub_cmdline
	initrd /initramfs.img
}

menuentry "Firmware UEFI" --class settings {
	fwsetup
}
EOF
	chown root:root /boot/efi/grub/grub.cfg \
		|| die "Could not chown GRUB config"
	chmod 0644 /boot/efi/grub/grub.cfg \
		|| die "Could not chmod GRUB config"
}

function configure_amd_gpu_acceleration() {
	install -d -m0755 -o root -g root /etc/environment.d \
		|| die "Could not create /etc/environment.d"
	cat > /etc/environment.d/90-amd-cezanne-gpu.conf <<'EOF'
# AMD Cezanne / Radeon Vega integrated GPU tuning.
LIBVA_DRIVER_NAME=radeonsi
VDPAU_DRIVER=radeonsi
AMD_VULKAN_ICD=RADV
MESA_VK_WSI_PRESENT_MODE=mailbox
EOF
	chown root:root /etc/environment.d/90-amd-cezanne-gpu.conf \
		|| die "Could not chown AMD GPU environment"
	chmod 0644 /etc/environment.d/90-amd-cezanne-gpu.conf \
		|| die "Could not chmod AMD GPU environment"
	ln -sfn 90-amd-cezanne-gpu.conf /etc/environment.d/90-amd-renoir-gpu.conf \
		|| cp -f /etc/environment.d/90-amd-cezanne-gpu.conf /etc/environment.d/90-amd-renoir-gpu.conf

	install -d -m0755 -o root -g root /etc/profile.d \
		|| die "Could not create /etc/profile.d"
	cat > /etc/profile.d/90-amd-cezanne-gpu.sh <<'EOF'
# AMD Cezanne / Radeon Vega integrated GPU tuning.
export LIBVA_DRIVER_NAME="${LIBVA_DRIVER_NAME:-radeonsi}"
export VDPAU_DRIVER="${VDPAU_DRIVER:-radeonsi}"
export AMD_VULKAN_ICD="${AMD_VULKAN_ICD:-RADV}"
export MESA_VK_WSI_PRESENT_MODE="${MESA_VK_WSI_PRESENT_MODE:-mailbox}"
EOF
	chown root:root /etc/profile.d/90-amd-cezanne-gpu.sh \
		|| die "Could not chown AMD GPU profile"
	chmod 0644 /etc/profile.d/90-amd-cezanne-gpu.sh \
		|| die "Could not chmod AMD GPU profile"
	ln -sfn 90-amd-cezanne-gpu.sh /etc/profile.d/90-amd-renoir-gpu.sh \
		|| cp -f /etc/profile.d/90-amd-cezanne-gpu.sh /etc/profile.d/90-amd-renoir-gpu.sh
}

function configure_unicode_fonts() {
	install -d -m0755 -o root -g root /etc/fonts \
		|| die "Could not create /etc/fonts"
	cat > /etc/fonts/local.conf <<'EOF'
<?xml version="1.0"?>
<!DOCTYPE fontconfig SYSTEM "urn:fontconfig:fonts.dtd">
<fontconfig>
	<alias>
		<family>sans-serif</family>
		<prefer>
			<family>Inter</family>
			<family>Noto Sans</family>
			<family>Noto Sans CJK JP</family>
			<family>Noto Sans CJK SC</family>
			<family>Noto Sans CJK TC</family>
			<family>Noto Sans CJK KR</family>
			<family>Noto Color Emoji</family>
		</prefer>
	</alias>
	<alias>
		<family>serif</family>
		<prefer>
			<family>Noto Serif</family>
			<family>Noto Serif CJK JP</family>
			<family>Noto Serif CJK SC</family>
			<family>Noto Serif CJK TC</family>
			<family>Noto Serif CJK KR</family>
			<family>Noto Color Emoji</family>
		</prefer>
	</alias>
	<alias>
		<family>monospace</family>
		<prefer>
			<family>JetBrains Mono</family>
			<family>Noto Sans Mono</family>
			<family>Noto Sans Mono CJK JP</family>
			<family>Noto Sans Mono CJK SC</family>
			<family>Noto Sans Mono CJK TC</family>
			<family>Noto Sans Mono CJK KR</family>
			<family>Noto Color Emoji</family>
		</prefer>
	</alias>
	<alias>
		<family>emoji</family>
		<prefer>
			<family>Noto Color Emoji</family>
		</prefer>
	</alias>
</fontconfig>
EOF
	chown root:root /etc/fonts/local.conf \
		|| die "Could not chown fontconfig local.conf"
	chmod 0644 /etc/fonts/local.conf \
		|| die "Could not chmod fontconfig local.conf"

	if command -v fc-cache > /dev/null 2>&1; then
		fc-cache -rs \
			|| ewarn "Could not rebuild font cache; it will be rebuilt later."
	fi
}

function verify_exact_sha256_manifest() {
	local source_directory="$1"
	local description="$2"
	local manifest="$source_directory/SHA256SUMS"
	local declared_files actual_files

	[[ -f "$manifest" ]] \
		|| die "$description checksum manifest was not found"
	declared_files="$(
		sed -n 's/^[[:xdigit:]]\{64\}  //p' "$manifest" | LC_ALL=C sort
	)"
	actual_files="$(
		cd "$source_directory" || die "Could not open $description source directory"
		find . -type f ! -path './SHA256SUMS' -print | LC_ALL=C sort
	)"
	[[ "$declared_files" == "$actual_files" ]] \
		|| die "$description checksum manifest does not cover the exact file set"
	(
		cd "$source_directory" || exit 1
		sha256sum --check --quiet --strict SHA256SUMS
	) || die "$description checksum verification failed"
}

function install_gentoo_hp_system_fonts() {
	local font_source="$GENTOO_INSTALL_REPO_DIR/contrib/fonts/inter"
	local font_target="/usr/local/share/fonts/gentoo-hp/inter"
	local license_target="/usr/local/share/doc/gentoo-hp/fonts"
	local font_file font_count
	local -a font_files=()

	[[ -d "$font_source" ]] \
		|| die "Gentoo-HP Inter font source was not found: $font_source"
	[[ -f "$font_source/LICENSE-Inter.txt" ]] \
		|| die "Gentoo-HP Inter font license was not found"
	if find "$font_source" -type l -print -quit | grep -q .; then
		die "Gentoo-HP font source must not contain symbolic links"
	fi
	verify_exact_sha256_manifest "$font_source" "Gentoo-HP Inter font"

	mapfile -d '' -t font_files \
		< <(find "$font_source" -maxdepth 1 -type f -name 'Inter*.ttf' -print0)
	font_count="${#font_files[@]}"
	[[ "$font_count" -eq 36 ]] \
		|| die "Expected 36 Inter/Inter Display fonts, found $font_count"

	einfo "Instalando Inter e Inter Display a nivel del sistema"
	install -d -m0755 -o root -g root "$font_target" "$license_target" \
		|| die "Could not create Gentoo-HP font directories"
	for font_file in "${font_files[@]}"; do
		if command -v fc-scan > /dev/null 2>&1; then
			fc-scan "$font_file" > /dev/null \
				|| die "Invalid font file: $font_file"
		fi
		install -m0644 -o root -g root "$font_file" \
				"$font_target/${font_file##*/}" \
			|| die "Could not install font: ${font_file##*/}"
	done
	install -m0644 -o root -g root \
			"$font_source/LICENSE-Inter.txt" \
			"$license_target/LICENSE-Inter.txt" \
		|| die "Could not install the Inter font license"
}

function configure_kde_desktop() {
	case "${INSTALL_KDE_CUSTOM_PROFILE,,}" in
		true)
			;;
		false)
			einfo "Omitiendo el perfil KDE personalizado por configuración."
			return 0
			;;
		*)
			die "INSTALL_KDE_CUSTOM_PROFILE must be true or false"
			;;
	esac

	[[ -n "$INSTALL_USER" ]] \
		|| die "A normal user is required for the KDE profile"

	local profile_source="$GENTOO_INSTALL_REPO_DIR/contrib/kde/orizaba/snapshot"
	local importer_source="$GENTOO_INSTALL_REPO_DIR/contrib/kde/orizaba/import_kde.sh"
	local first_login_source="$GENTOO_INSTALL_REPO_DIR/contrib/kde/orizaba/first_login.sh"
	local autostart_source="$GENTOO_INSTALL_REPO_DIR/contrib/kde/orizaba/gentoo-hp-kde-first-login.desktop"
	local profile_target="/usr/local/share/gentoo-hp/kde/orizaba"
	local importer_target="/usr/local/bin/gentoo-hp-apply-kde"
	local first_login_target="/usr/local/bin/gentoo-hp-kde-first-login"
	local autostart_target="/etc/xdg/autostart/gentoo-hp-kde-first-login.desktop"
	local install_user_home="/home/$INSTALL_USER"
	local profile_digest

	[[ -d "$profile_source" && -f "$profile_source/SHA256SUMS" ]] \
		|| die "Gentoo-HP KDE profile is incomplete: $profile_source"
	[[ -x "$importer_source" ]] \
		|| die "Gentoo-HP KDE importer is missing or not executable"
	[[ -x "$first_login_source" && -f "$autostart_source" ]] \
		|| die "Gentoo-HP KDE first-login integration is incomplete"
	command -v rsync > /dev/null 2>&1 \
		|| die "Gentoo-HP KDE profile requires net-misc/rsync"
	if find "$profile_source" -type l -print -quit | grep -q .; then
		die "Gentoo-HP KDE profile must not contain symbolic links"
	fi
	verify_exact_sha256_manifest "$profile_source" "Gentoo-HP KDE profile"

	einfo "Instalando el perfil KDE Orizaba saneado"
	[[ ! -L "$profile_target" ]] \
		|| die "Refusing symbolic link at the KDE profile target: $profile_target"
	install -d -m0755 -o root -g root \
			/etc/xdg/autostart \
			/usr/local/bin \
			/usr/local/share/gentoo-hp/kde \
			"$profile_target" \
		|| die "Could not create Gentoo-HP KDE profile directories"
	rsync -a --delete "$profile_source/" "$profile_target/" \
		|| die "Could not install the Gentoo-HP KDE profile"
	chown -R root:root "$profile_target" \
		|| die "Could not set KDE profile ownership"
	find "$profile_target" -type d -exec chmod 0755 '{}' + \
		|| die "Could not set KDE profile directory modes"
	find "$profile_target" -type f -exec chmod 0644 '{}' + \
		|| die "Could not set KDE profile file modes"
	install -m0755 -o root -g root "$importer_source" "$importer_target" \
		|| die "Could not install the Gentoo-HP KDE importer"
	install -m0755 -o root -g root "$first_login_source" "$first_login_target" \
		|| die "Could not install the Gentoo-HP KDE first-login helper"
	install -m0644 -o root -g root "$autostart_source" "$autostart_target" \
		|| die "Could not install the Gentoo-HP KDE autostart entry"

	profile_digest="$(sha256sum "$profile_target/SHA256SUMS" | awk '{print $1}')" \
		|| die "Could not calculate the KDE profile digest"

	runuser -u "$INSTALL_USER" -- env \
			HOME="$install_user_home" \
			USER="$INSTALL_USER" \
			LOGNAME="$INSTALL_USER" \
			XDG_CONFIG_HOME="$install_user_home/.config" \
			XDG_DATA_HOME="$install_user_home/.local/share" \
			XDG_STATE_HOME="$install_user_home/.local/state" \
			"$importer_target" \
				--source "$profile_target" \
				--no-restart \
				--profile-digest "$profile_digest" \
				--skip-if-applied \
		|| die "Could not apply the Gentoo-HP KDE profile"
}

function configure_system_identity() {
	[[ "$SYSTEMD" == "true" ]] \
		|| return 0
	[[ -n "${PRETTY_HOSTNAME-}" ]] \
		|| return 0
	[[ "$PRETTY_HOSTNAME" != *$'\n'* && "$PRETTY_HOSTNAME" != *$'\r'* ]] \
		|| die "PRETTY_HOSTNAME must not contain line breaks"

	einfo "Configurando nombre visible del equipo: $PRETTY_HOSTNAME"
	local pretty_hostname_escaped="$PRETTY_HOSTNAME"
	pretty_hostname_escaped="${pretty_hostname_escaped//\\/\\\\}"
	pretty_hostname_escaped="${pretty_hostname_escaped//\"/\\\"}"
	pretty_hostname_escaped="${pretty_hostname_escaped//\$/\\\$}"
	pretty_hostname_escaped="${pretty_hostname_escaped//\`/\\\`}"

	touch_or_die 0644 /etc/machine-info
	sed -i '/^[[:space:]]*PRETTY_HOSTNAME=/d' /etc/machine-info \
		|| die "Could not update /etc/machine-info"
	printf 'PRETTY_HOSTNAME="%s"\n' "$pretty_hostname_escaped" >> /etc/machine-info \
		|| die "Could not write PRETTY_HOSTNAME"
	chown root:root /etc/machine-info \
		|| die "Could not chown /etc/machine-info"
	chmod 0644 /etc/machine-info \
		|| die "Could not chmod /etc/machine-info"
}

function configure_freetype_rendering() {
	[[ -n "${FREETYPE_PROPERTIES_VALUE-}" ]] \
		|| return 0

	einfo "Configurando stem darkening de FreeType"
	if [[ ! -e /etc/environment ]]; then
		install -m0644 -o root -g root /dev/null /etc/environment \
			|| die "Could not create /etc/environment"
	fi
	sed -i '/^[[:space:]]*FREETYPE_PROPERTIES=/d' /etc/environment \
		|| die "Could not update /etc/environment"
	printf 'FREETYPE_PROPERTIES="%s"\n' "$FREETYPE_PROPERTIES_VALUE" >> /etc/environment \
		|| die "Could not write FREETYPE_PROPERTIES to /etc/environment"
	chown root:root /etc/environment \
		|| die "Could not chown /etc/environment"
	chmod 0644 /etc/environment \
		|| die "Could not chmod /etc/environment"

	install -d -m0755 -o root -g root /etc/env.d \
		|| die "Could not create /etc/env.d"
	printf 'FREETYPE_PROPERTIES="%s"\n' "$FREETYPE_PROPERTIES_VALUE" \
		> /etc/env.d/99gentoo-hp-font-rendering \
		|| die "Could not write Gentoo FreeType environment"
	chown root:root /etc/env.d/99gentoo-hp-font-rendering \
		|| die "Could not chown Gentoo FreeType environment"
	chmod 0644 /etc/env.d/99gentoo-hp-font-rendering \
		|| die "Could not chmod Gentoo FreeType environment"

	# Gentoo sessions consume /etc/profile.env, generated from /etc/env.d.
	env_update
}

function configure_sway_desktop() {
	[[ -n "$INSTALL_USER" ]] \
		|| return 0

	local install_user_group install_user_home profile_root session_root
	install_user_home="/home/$INSTALL_USER"
	profile_root="$GENTOO_INSTALL_REPO_DIR/contrib/mysway/rootfs"
	session_root="$GENTOO_INSTALL_REPO_DIR/contrib/mysway/session"
	install_user_group="$(id -gn "$INSTALL_USER")" \
		|| die "Could not determine primary group for '$INSTALL_USER'"

	[[ -f "$profile_root/.config/sway/config" ]] \
		|| die "MySway profile not found: $profile_root"
	[[ -x "$profile_root/.config/sway/session.sh" ]] \
		|| die "MySway user session wrapper is missing or not executable"
	[[ -x "$session_root/mysway-session" && -f "$session_root/mysway.desktop" ]] \
		|| die "MySway SDDM session files are incomplete"
	if find "$profile_root" -type f -path '*/quickshell/*' -print -quit | grep -q .; then
		die "MySway profile must not contain Quickshell"
	fi

	einfo "Instalando MySway modular con Kitty y sesión aislada de Plasma"
	install -d -m0755 -o "$INSTALL_USER" -g "$install_user_group" \
			"$install_user_home/.config" \
			"$install_user_home/.local" \
		|| die "Could not create MySway user directories"
	cp -a "$profile_root/.config/." "$install_user_home/.config/" \
		|| die "Could not install MySway configuration"
	cp -a "$profile_root/.local/." "$install_user_home/.local/" \
		|| die "Could not install MySway helper scripts"
	chmod 0755 \
			"$install_user_home/.config/sway/session.sh" \
			"$install_user_home/.local/bin"/mysway-* \
		|| die "Could not make MySway scripts executable"
	chown -R "$INSTALL_USER:$install_user_group" \
			"$install_user_home/.config/sway" \
			"$install_user_home/.config/waybar" \
			"$install_user_home/.config/wofi" \
			"$install_user_home/.config/mako" \
			"$install_user_home/.config/kitty" \
			"$install_user_home/.local/bin" \
		|| die "Could not set MySway profile ownership"

	install -Dm0755 "$session_root/mysway-session" /usr/local/bin/mysway-session \
		|| die "Could not install the MySway SDDM wrapper"
	install -Dm0644 "$session_root/mysway.desktop" \
			/usr/share/wayland-sessions/mysway.desktop \
		|| die "Could not install the MySway SDDM entry"
}

function configure_xdg_user_directories() {
	[[ -n "$INSTALL_USER" ]] \
		|| return 0

	local install_user_group
	local install_user_home="/home/$INSTALL_USER"
	local -a user_directories=(
		"$install_user_home/Escritorio"
		"$install_user_home/Descargas"
		"$install_user_home/Documentos"
		"$install_user_home/Imágenes"
		"$install_user_home/Música"
		"$install_user_home/Vídeos"
	)

	install_user_group="$(id -gn "$INSTALL_USER")" \
		|| die "Could not determine primary group for '$INSTALL_USER'"

	einfo "Creando directorios personales XDG en español"
	install -d -m0755 -o "$INSTALL_USER" -g "$install_user_group" \
			"$install_user_home/.config" \
			"${user_directories[@]}" \
		|| die "Could not create XDG user directories"

	cat > "$install_user_home/.config/user-dirs.dirs" <<'EOF'
# Directorios personales XDG administrados por Gentoo-HP.
XDG_DESKTOP_DIR="$HOME/Escritorio"
XDG_DOWNLOAD_DIR="$HOME/Descargas"
XDG_TEMPLATES_DIR="$HOME"
XDG_PUBLICSHARE_DIR="$HOME"
XDG_DOCUMENTS_DIR="$HOME/Documentos"
XDG_MUSIC_DIR="$HOME/Música"
XDG_PICTURES_DIR="$HOME/Imágenes"
XDG_VIDEOS_DIR="$HOME/Vídeos"
EOF
	printf '%s\n' 'es_MX' > "$install_user_home/.config/user-dirs.locale" \
		|| die "Could not write XDG user directory locale"

	chown "$INSTALL_USER:$install_user_group" \
			"$install_user_home/.config/user-dirs.dirs" \
			"$install_user_home/.config/user-dirs.locale" \
		|| die "Could not chown XDG user directory configuration"
	chmod 0644 \
			"$install_user_home/.config/user-dirs.dirs" \
			"$install_user_home/.config/user-dirs.locale" \
		|| die "Could not chmod XDG user directory configuration"
}

function configure_flatpak() {
	command -v flatpak > /dev/null 2>&1 \
		|| die "Flatpak was requested but is not installed"

	einfo "Configurando Flathub para Discover y Flatpak"
	try flatpak remote-add --system --if-not-exists \
		flathub https://flathub.org/repo/flathub.flatpakrepo

	einfo "Instalando la extension GTK Breeze para aplicaciones Flatpak"
	try flatpak install --system --noninteractive --or-update --runtime \
		flathub org.gtk.Gtk3theme.Breeze//3.22

	if [[ -n "$INSTALL_USER" ]]; then
		local install_user_group
		local install_user_home="/home/$INSTALL_USER"
		install_user_group="$(id -gn "$INSTALL_USER")" \
			|| die "Could not determine primary group for '$INSTALL_USER'"

		install -d -m0755 -o "$INSTALL_USER" -g "$install_user_group" \
				"$install_user_home/.config/gtk-3.0" \
				"$install_user_home/.local/share" \
			|| die "Could not create Flatpak desktop integration directories"

		# Breeze's generated accent CSS lives here. Keep the permission
		# read-only and limited to GTK3 instead of exposing the whole home.
		einfo "Compartiendo la configuracion GTK3 de Breeze con Flatpak en modo lectura"
		try runuser -u "$INSTALL_USER" -- env \
			HOME="$install_user_home" \
			USER="$INSTALL_USER" \
			LOGNAME="$INSTALL_USER" \
			XDG_CONFIG_HOME="$install_user_home/.config" \
			XDG_DATA_HOME="$install_user_home/.local/share" \
			flatpak override --user --filesystem=xdg-config/gtk-3.0:ro
	fi
}

function configure_audio_bluetooth() {
	install -d -m0755 -o root -g root \
			/etc/wireplumber/wireplumber.conf.d \
		|| die "Could not create WirePlumber configuration directory"
	install -m0644 -o root -g root \
			"$GENTOO_INSTALL_REPO_DIR/contrib/wireplumber/10-gentoo-hp-audio-bluetooth.conf" \
			/etc/wireplumber/wireplumber.conf.d/10-gentoo-hp-audio-bluetooth.conf \
		|| die "Could not install WirePlumber audio/Bluetooth configuration"
}

function configure_virtualization() {
	local default_network_autostart="/etc/libvirt/qemu/networks/autostart/default.xml"

	install -d -m0755 -o root -g root \
			/etc/modules-load.d \
			/etc/sysctl.d \
			/etc/libvirt/qemu/networks/autostart \
		|| die "Could not create virtualization configuration directories"
	install -m0644 -o root -g root \
			"$GENTOO_INSTALL_REPO_DIR/contrib/modules-load/90-gentoo-hp-virtualization.conf" \
			/etc/modules-load.d/90-gentoo-hp-virtualization.conf \
		|| die "Could not install virtualization modules-load configuration"
	install -m0644 -o root -g root \
			"$GENTOO_INSTALL_REPO_DIR/contrib/sysctl/90-gentoo-hp-virtualization.conf" \
			/etc/sysctl.d/90-gentoo-hp-virtualization.conf \
		|| die "Could not install virtualization sysctl configuration"

	if [[ ! -f /etc/libvirt/qemu/networks/default.xml \
			&& -f /usr/share/libvirt/networks/default.xml ]]; then
		install -m0644 -o root -g root \
				/usr/share/libvirt/networks/default.xml \
				/etc/libvirt/qemu/networks/default.xml \
			|| die "Could not install libvirt default network"
	fi
	if [[ -f /etc/libvirt/qemu/networks/default.xml ]]; then
		if [[ -L "$default_network_autostart" ]]; then
			ln -sfn ../default.xml "$default_network_autostart" \
				|| die "Could not refresh libvirt default network autostart"
		elif [[ ! -e "$default_network_autostart" ]]; then
			ln -s ../default.xml "$default_network_autostart" \
				|| die "Could not enable libvirt default network autostart"
		else
			einfo "Preserving existing libvirt default network autostart configuration."
		fi
	else
		ewarn "Libvirt default network XML was not found; define it after installation."
	fi
}

function finalize_user_home_ownership() {
	[[ -n "$INSTALL_USER" ]] \
		|| return 0
	[[ -d "/home/$INSTALL_USER" ]] \
		|| die "User home does not exist: /home/$INSTALL_USER"

	local install_user_group
	install_user_group="$(id -gn "$INSTALL_USER")" \
		|| die "Could not determine primary group for '$INSTALL_USER'"

	einfo "Corrigiendo propietario de /home/$INSTALL_USER"
	chown -R "$INSTALL_USER:$install_user_group" "/home/$INSTALL_USER" \
		|| die "Could not set ownership for /home/$INSTALL_USER"
}

function after_install() {
	configure_admin_user
	configure_user_bashrc
	configure_sddm
	configure_system_identity
	configure_grub_bootloader
	configure_amd_gpu_acceleration
	install_gentoo_hp_system_fonts
	configure_unicode_fonts
	configure_freetype_rendering
	configure_kde_desktop
	configure_sway_desktop
	configure_xdg_user_directories
	configure_audio_bluetooth
	configure_virtualization
	configure_flatpak
	finalize_user_home_ownership

	if [[ $SYSTEMD == "true" ]]; then
		try systemctl --global enable pipewire.socket
		try systemctl --global enable pipewire-pulse.socket
		try systemctl --global enable wireplumber.service
		try systemctl enable bluetooth.service
		try systemctl enable libvirtd.service
		try systemctl enable libvirtd.socket
		try systemctl enable libvirtd-ro.socket
		try systemctl enable virtlockd.socket
		try systemctl enable virtlogd.socket
		try systemctl enable libvirt-guests.service
		try systemctl enable NetworkManager
		try systemctl enable iwd
		try systemctl enable sddm.service
		try systemctl enable power-profiles-daemon.service
	fi
}


################################################
# Prove that you have read the config

# Auto-NVMe mode is enabled. The installer still shows the disk layout before
# partitioning; confirm only after checking the selected NVMe.
I_HAVE_READ_AND_EDITED_THE_CONFIG_PROPERLY=true
